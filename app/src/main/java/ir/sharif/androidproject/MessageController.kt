package ir.sharif.androidproject

object MessageController {
    private val cache = mutableListOf<Int>()

    fun fetch(fromCache: Boolean = false) {
        TODO()
//        NotificationCenter.notifySubscribers(Notification(NotificationType.DATA_LOADED))
    }
}
